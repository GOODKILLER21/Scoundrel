workflows:
  ios-capacitor-build:
    name: Scoundrel iOS Build
    environment:
      node: latest
    scripts:
      - npm install
      - npm run build
      - npx cap sync ios
    artifacts:
      - ios/**/*.ipa